{
  "name": "openclaw-plugin-bosch-smarthome",
  "version": "0.1.0",
  "type": "module",
  "description": "OpenClaw plugin for Bosch Smart Home Controller local API",
  "private": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "plugin:build": "npm run build && openclaw plugins build --entry ./dist/index.js",
    "plugin:validate": "npm run build && openclaw plugins validate --entry ./dist/index.js",
    "test": "vitest run"
  },
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "peerDependencies": {
    "openclaw": ">=2026.5.17"
  },
  "dependencies": {
    "typebox": "^1.3.0",
    "bosch-smart-home-bridge": "^1.8.1"
  },
  "devDependencies": {
    "openclaw": "latest",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
